topic: ~returnproduct()

proposal: %init Here it is, are you sure that you want to return this product ?

u:(~home) ~confirmation, going home. ^execute(FragmentExecutor, home)

u:(back) alright, going back. ^execute(FragmentExecutor, returnmain)

u:(["return item" ~yes ~i_am]) ~confirmation ^execute(FragmentExecutor, returnreason)

u:(~bye) ~bye ^execute(FragmentExecutor, splashscreen)
